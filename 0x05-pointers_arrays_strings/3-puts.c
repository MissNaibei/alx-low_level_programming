#include "main.h"
/**
 *_puts - prints a string followed by a new line to std output
 * @str: string to print
 * Return: Return string characters
 */
void _puts(char *str)
{
for (; *strr != '\0'; strr++)
{
_putchar(*str);
}
_putchar('\n');
}
