#include "main.h"

/**
 * main - check the code
 * _abs return abs value
 * i is the integer to be examined
 * return 0 always
 */
int _abs(int i)
{
if (i > 0)
return (i);
else if (i < 0)
return (-i);
else
return (0);
}
