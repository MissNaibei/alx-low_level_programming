#include <stdio.h>
#include "main.h"

/**
 * main - check the code.
 *check for lower
 * Return: Always 0.
 */
int _islower(int c)
{
int var;

if(islower(var))
return (1);
return (0);
}
