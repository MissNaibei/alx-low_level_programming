#include "main.h"
/**
 * main - check the code
 * print_last_digit - prints the last digit
 * @a - number that is targated
 * Return: 0 success
 */

int print_last_digit(int a)
{
last_digit = a % 10
return (last_digit);
}
